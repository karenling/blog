!!!
%html
  %head
    %title= @title ? "Twinkie and Karen | #{@title}" : "Twinkie and Karen"
    %link{href: "https://fonts.googleapis.com/css?family=Lato:300,300i,400,400i,700,700i",
          rel: "stylesheet",
          type: "text/css"}/
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    %meta{:content => "width=device-width, initial-scale=1", :name => "viewport"}/
%body
  .container-fluid
    = render partial: 'layouts/flash'
    .row
      = yield
  %footer.container-fluid= copyright_text


- if @session_timezone.blank?
  :javascript
    TwinkieandKaren.setTimezone();
